apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "todo-app.fullname" . }}-frontend
  labels:
    {{- include "todo-app.frontend.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount.frontend }}
  selector:
    matchLabels:
      {{- include "todo-app.frontend.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "todo-app.frontend.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.securityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
      - name: frontend
        image: "{{ .Values.image.frontend.repository }}:{{ .Values.image.frontend.tag }}"
        imagePullPolicy: {{ .Values.image.frontend.pullPolicy }}
        ports:
        - name: http
          containerPort: {{ .Values.service.frontend.targetPort }}
          protocol: TCP
        env:
        # Environment variables from ConfigMap
        - name: BETTER_AUTH_URL
          valueFrom:
            configMapKeyRef:
              name: {{ include "todo-app.fullname" . }}-config
              key: BETTER_AUTH_URL
        - name: FRONTEND_URL
          valueFrom:
            configMapKeyRef:
              name: {{ include "todo-app.fullname" . }}-config
              key: FRONTEND_URL
        - name: BACKEND_URL
          valueFrom:
            configMapKeyRef:
              name: {{ include "todo-app.fullname" . }}-config
              key: BACKEND_URL
        - name: NEXT_PUBLIC_CHATKIT_API_URL
          valueFrom:
            configMapKeyRef:
              name: {{ include "todo-app.fullname" . }}-config
              key: NEXT_PUBLIC_CHATKIT_API_URL
        # Environment variables from Secret
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: {{ include "todo-app.fullname" . }}-secrets
              key: DATABASE_URL
        - name: BETTER_AUTH_SECRET
          valueFrom:
            secretKeyRef:
              name: {{ include "todo-app.fullname" . }}-secrets
              key: BETTER_AUTH_SECRET
        - name: OPENAI_API_KEY
          valueFrom:
            secretKeyRef:
              name: {{ include "todo-app.fullname" . }}-secrets
              key: OPENAI_API_KEY
        {{- if .Values.secrets.CLOUDFLARE_R2_ACCOUNT_ID }}
        - name: CLOUDFLARE_R2_ACCOUNT_ID
          valueFrom:
            secretKeyRef:
              name: {{ include "todo-app.fullname" . }}-secrets
              key: CLOUDFLARE_R2_ACCOUNT_ID
        {{- end }}
        {{- if .Values.secrets.CLOUDFLARE_R2_ACCESS_KEY_ID }}
        - name: CLOUDFLARE_R2_ACCESS_KEY_ID
          valueFrom:
            secretKeyRef:
              name: {{ include "todo-app.fullname" . }}-secrets
              key: CLOUDFLARE_R2_ACCESS_KEY_ID
        {{- end }}
        {{- if .Values.secrets.CLOUDFLARE_R2_SECRET_ACCESS_KEY }}
        - name: CLOUDFLARE_R2_SECRET_ACCESS_KEY
          valueFrom:
            secretKeyRef:
              name: {{ include "todo-app.fullname" . }}-secrets
              key: CLOUDFLARE_R2_SECRET_ACCESS_KEY
        {{- end }}
        {{- if .Values.secrets.CLOUDFLARE_R2_BUCKET_NAME }}
        - name: CLOUDFLARE_R2_BUCKET_NAME
          valueFrom:
            secretKeyRef:
              name: {{ include "todo-app.fullname" . }}-secrets
              key: CLOUDFLARE_R2_BUCKET_NAME
        {{- end }}
        # Health Probes
        readinessProbe:
          httpGet:
            path: {{ .Values.healthProbe.frontend.readiness.path }}
            port: {{ .Values.healthProbe.frontend.readiness.port }}
          initialDelaySeconds: {{ .Values.healthProbe.frontend.readiness.initialDelaySeconds }}
          periodSeconds: {{ .Values.healthProbe.frontend.readiness.periodSeconds }}
          timeoutSeconds: {{ .Values.healthProbe.frontend.readiness.timeoutSeconds }}
          failureThreshold: {{ .Values.healthProbe.frontend.readiness.failureThreshold }}
        livenessProbe:
          httpGet:
            path: {{ .Values.healthProbe.frontend.liveness.path }}
            port: {{ .Values.healthProbe.frontend.liveness.port }}
          initialDelaySeconds: {{ .Values.healthProbe.frontend.liveness.initialDelaySeconds }}
          periodSeconds: {{ .Values.healthProbe.frontend.liveness.periodSeconds }}
          timeoutSeconds: {{ .Values.healthProbe.frontend.liveness.timeoutSeconds }}
          failureThreshold: {{ .Values.healthProbe.frontend.liveness.failureThreshold }}
        resources:
          {{- toYaml .Values.resources.frontend | nindent 10 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
